# AGENTS.md

本文件适用于项目根目录及其全部子目录。所有参与本项目的 AI Agent 在分析、设计和修改代码时，必须遵守以下约束。

## 规范入口与维护

- `AGENTS.md` 维护项目级修改流程、技术环境、验证和交付约束；`.agents/skills/partner-workbench-ui/SKILL.md` 维护页面开发流程；该技能下的 `references/design-system.md` 维护具体视觉、交互和审核意见数据契约。相同规则优先在所属文档维护，其他文件引用，避免复制后不一致。
- 页面开发开始前读取本文件和技能入口，按技能指引读取设计规范，并检查目标页面及复用组件的当前实现。现有代码用于定位复用点，不代表其中所有历史样式都符合规范。
- 用户当前明确需求优先；没有明确例外时遵循项目规范。原型保留产品功能和内容，通用视觉遵循设计系统；发现无法兼容的业务要求时说明具体冲突，不擅自删减功能。
- 用户要求沉淀规范时，将可复用规则写入设计系统，并标明适用页面、模式和例外。单页规则不得无条件推广至所有页面；规范变更不代表授权批量修改历史页面。
- 文档维护后检查相互引用、示例与正文是否一致；区分规范要求、已核对的实现和待人工验收项，不以文档校验代替页面验收。

## 修改流程

- 每次准备修改项目文件前，必须先向用户提供修改方案，说明拟修改的文件、内容、影响范围及人工验证要点。
- 修改方案必须以实现当前需求所需的最小改动为原则，不得顺带重构、优化或调整与当前需求无关的代码、结构、依赖和样式。
- 提供方案后必须等待用户给出明确的执行指令；未获得明确同意前，只能进行只读检查、分析和方案说明，不得编辑、新增、删除、移动或覆盖任何项目文件。
- 用户对方案提出调整时，应先更新方案并再次等待明确指令，不得依据未确认的旧方案开始修改。
- 用户已经针对明确文件和明确修改内容直接给出“修改”“增加”“删除”“执行”等指令时，可视为该项修改的明确授权；仍应在执行前简要说明最小修改方案。

## UI 设计

- 新建伙伴端页面，或修改伙伴端页面的 UI、样式与版式时，必须使用项目技能 `$partner-workbench-ui`，并遵循其中的工作台设计系统约束。
- 当需求涉及全新的页面、模块、组件外观或视觉方案时，优先使用 Canva 插件完成设计或产出设计参考，再进行前端实现。
- 如果 Canva 插件不可用、未安装或无法完成当前设计任务，应明确说明原因，并基于现有项目视觉风格继续实现，不得因此擅自改变需求范围。
- 对现有 UI 的小范围修正（如文案、颜色、间距、对齐和尺寸调整）无需先使用 Canva。
- 有原型时，UI 设计与前端实现必须以原型为准。原型中的功能、标题和模块均视为需求内容，不得擅自删除、合并、隐藏或省略。
- 原型中仅用于说明设计意图的尺寸标注、按钮示例、状态示例、辅助文字、参考线等演示性内容，不得原样保留在最终成品中。
- 实现时应区分“产品内容”和“原型注释或示例”：无法确定某项内容是否属于正式功能时，应保留其对应功能，并在交付说明中标注待人工确认，不得自行删减。

## 前端技术标准

- 项目开发与构建环境统一使用 Node.js `10.24.x`，推荐通过根目录 `.nvmrc` 切换到 `10.24.0`；不得在未评估兼容性的情况下升级 Node 版本。
- 构建链使用 Webpack 4 和 `pnpm@5.18.10`，统一通过 `pnpm install --frozen-lockfile` 安装；必须保留 `.npmrc` 的 `engine-strict=true`。不得重新引入不支持 Node.js 10.24 的 Vite 或其他构建依赖。
- 新增或升级直接、间接依赖时，必须验证 Node.js 10.24 的引擎要求、语法和运行时 API，并在实际 Node.js 10.24 下完成干净安装、完整构建和服务启动验证；不得仅修改 `engines` 声明。浏览器端 API 与 Node 端 API 应按执行环境分别判断。
- 前端必须按照 **Vue 2.6 + Ant Design Vue** 标准开发。
- 当前项目基准版本为 `vue@2.6.14` 和 `ant-design-vue@1.7.8`，新增或修改的代码必须与这两个版本兼容。
- Vue 组件使用 Vue 2 Options API，包括 `data`、`computed`、`methods`、`watch`、`props` 和生命周期钩子。
- 不得使用 Vue 3 专属语法或 API，例如 Composition API、`setup`、`<script setup>`、Teleport、Fragments、`createApp` 等。
- Ant Design Vue 组件、属性、事件和插槽写法必须以 1.7.x API 为准，不得照搬 Ant Design Vue 2.x、3.x 或 4.x 的用法。
- 优先复用 Ant Design Vue 现有组件及项目已有的公共组件、样式和布局模式，避免无必要地引入新的 UI 框架或重复实现基础组件。
- 保持现有工程结构、命名方式和编码风格；除非需求明确要求，不进行与任务无关的框架升级或大规模重构。

## 线上环境对齐与本地运行

- 线上已知 Node 基线为 `10.24.x`；本地固定使用 `.nvmrc` 指定的 `10.24.0` 和 `pnpm@5.18.10`。如后续提供线上精确补丁版本，应先说明差异并按修改流程统一版本约束，不得自行升级。
- 每次安装、构建、启动或源码包验证前，必须在实际执行命令的同一终端中核实 `node --version`、`pnpm --version` 和可执行文件路径。不能只依据 `.nvmrc`、`engines` 或历史验证记录判断环境正确；子进程也必须继承该 Node/PATH。
- 使用项目专用运行时或当前终端的 PATH 切换，不得为本项目擅自修改系统全局 Node、pnpm 或其他项目环境。当前电脑可先执行 `. C:\Users\zhang\.codex\project-runtimes\start-see-node10.ps1`，再使用 `pnpm.cmd` 执行命令；该脚本是本机便利入口，使用前仍须检查其存在及实际版本，不能作为源码包的必要依赖。
- 若专用运行时缺失，应配置隔离的 Node `10.24.0` 和 pnpm `5.18.10`；Node 官方下载包须核对官方 SHA256。无法获得兼容环境时，明确报告阻塞，禁止退回高版本运行并声称验证通过。
- 禁止关闭 `engine-strict`、忽略引擎限制、绕过 `check:env` 或直接调用 Webpack 来规避检查。禁止用新版 pnpm、npm 或 Yarn 安装项目依赖或改写锁文件；安装专用 pnpm 工具本身不属于项目依赖安装。
- 用户要求“本地运行”时，默认表示按线上已知版本进行生产构建与本地仿真预览：在正确环境下依次执行 `pnpm install --frozen-lockfile`、`pnpm run build`、`pnpm run preview`。用户明确要求开发模式或热更新时才改用 `pnpm run dev`，同样不得绕过环境检查。
- 环境命令有一步失败即停止后续依赖步骤，修复后重新验证；不得在构建失败后启动旧的 `dist` 并报告本次运行成功。依赖首次安装、来源不明或环境切换后，须在独立临时目录从空 `node_modules` 进行冻结锁文件安装和完整构建验证；保留用户已有目录和修改。
- 启动前检查端口和现有进程，只能停止已确认属于本项目且需要替换的服务，不得终止无关服务。开发服务与预览服务默认均使用 `127.0.0.1:4173`，不可同时占用；已有服务只有在运行时、模式和产物均符合本次请求时才可复用。
- 必须等待编译或预览启动成功，确认实际服务进程使用 Node `10.24.0`，并通过非浏览器自动化的 HTTP 请求验证首页、入口 JavaScript 和代表性静态资源正常返回；不能仅凭进程存在或打印地址判定成功。交付时说明运行版本、模式、访问地址、验证结果和仍需人工检查的页面交互。
- 本地仿真覆盖已知 Node/pnpm、依赖安装、构建及静态资源服务。本项目是浏览器端应用，不包含 Node 后端或 SSR；本地 Windows 预览不能等同于复现线上操作系统、Web 服务器、反向代理、接口、数据库或权限配置。未提供的线上条件应明确列为未验证，不得虚构或连接生产数据。

## 审核意见设计与实现规范

- 新增或修改项目申请、项目修订、进展报告、结项报告及其他审核流程中的审核意见功能时，必须遵循 `.agents/skills/partner-workbench-ui/references/design-system.md` 第 13 节“审核意见通用设计与前端实现规范”。
- 项目基本信息等字段、富文本内容和表格明细行必须复用该章节规定的交互入口、查看方式、标红状态、提示文案、键盘操作与审核意见弹窗，不得为单独页面创建不一致的设计或交互。
- 前后端统一通过业务 JSON 顶层的 `reviewComments` 对象传递审核意见，并遵循稳定字段键、稳定行 ID、行级键与单元格级键的命名规则。不得使用数组下标、仅存在于 DOM 的状态或硬编码样式关联审核意见。
- 审核页负责新增、修改和删除意见；修改页负责读取、自动标红和查看意见；普通查看页默认不显示审核入口或审核标红。新增和删除必须使用 Vue 2.6 响应式兼容的 `$set` 与 `$delete`。
- 新增审核模块前应优先抽取或复用现有审核意见组件及公共方法。若现有实现与第 13 节冲突，应以该章节的数据契约和交互规则为准，并以完成当前需求所需的最小范围修正。
- 交付时必须按第 13.9 节检查 JSON 回写、刷新恢复、字段/富文本/表格状态、稳定行 ID、键盘操作、操作按钮事件隔离及无意见时隐藏提示等关键场景。

## UI 修改验证

- 仅涉及前端 UI、样式或版式调整的任务，不要求新增或运行自动化测试。
- Agent 不得执行视觉测试，不得使用浏览器自动化、截图对比或类似方式代替人工验收。
- 页面交互由用户人工验证；Agent 交付时应说明需要人工检查的关键页面、状态和交互步骤。
- 如果修改同时影响业务逻辑、数据处理、接口调用、权限或构建配置，则不属于纯 UI 调整，应根据影响范围进行必要的技术验证。

## 打包交付

- 只有当用户明确要求“打包文件”时才创建压缩包；普通开发或 UI 修改任务不得自动打包。
- “打包文件”是指生成可供开发人员继续进行业务逻辑开发的完整前端源码包，不得仅打包 `dist`、静态预览文件或单个页面。
- 源码包必须包含全部页面与组件源码、公共样式、静态资源、入口文件、依赖清单及锁文件、构建配置，以及继续开发所需的项目级说明和 `.agents` 约束。
- 不得打包 `node_modules`、`dist`、缓存、日志、版本控制目录、历史压缩包、打包输出目录或包含密钥的环境配置。需要说明的环境变量应通过不含真实密钥的示例文件提供。
- 所有打包产物统一放入项目根目录下独立的 `packages/<包名>/` 目录。压缩包使用可识别的项目源码包名称，并避免覆盖已有产物。
- 创建压缩包后应检查压缩包可正常读取，并确认其中存在 `package.json`、锁文件、`src` 全部源码、`public` 静态资源和必要构建配置；交付时提供压缩包的具体路径。
- 用户要求“打包文件”时，必须同时遵守上述线上环境对齐规则，默认交付经过 Node `10.24.0` + pnpm `5.18.10` 验证的完整源码包；仅讨论或修改本约定不视为立即打包指令。
- 包中必须保留 `.nvmrc`、`.npmrc`、`package.json` 的 `engines` 与 `pnpm.overrides`、原始 `pnpm-lock.yaml`、`webpack.config.js`、`scripts/`、根目录 `AGENTS.md` 和 `.agents/`。打包说明须包含准确版本、冻结锁文件安装、构建、生产预览及开发启动命令；不能依赖本机用户绝对路径、外部启动脚本或已安装依赖才能继续开发。
- 必须将最终压缩包解压到新的独立临时目录，在该目录用指定 Node/pnpm 从空 `node_modules` 执行 `pnpm install --frozen-lockfile`、`pnpm run build`，并启动 `pnpm run preview` 完成首页、入口 JavaScript 和代表性静态资源 HTTP 验证。可以复用 pnpm 缓存，但不得复制已有 `node_modules` 冒充干净安装；缓存不齐应联网按锁文件安装。临时验证生成的依赖和 `dist` 不得回填进源码包。
- 验证压缩包文件清单与拟交付源码一致、无排除项及真实密钥，核对解压后锁文件与源码锁文件一致且安装未改写它。验证过程若修改了源码包内容，必须重新打包并对最终包重新验证；不能只验证工作目录或引用历史成功记录。
- 任一安装、构建、预览或包完整性检查失败时，不得宣称源码包已通过线上版本验证；说明失败原因及未完成项。交付说明须列出源码包路径、SHA256、实际 Node/pnpm 版本、干净安装/构建/HTTP 验证结果、已知警告及人工验收要点，并说明线上未知条件的验证边界。
